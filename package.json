{
  "name": "app-mono-test",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:leandro-andrade-candido/app-mono-test.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start:dev": "yarn workspace auth-service start:dev & yarn workspace profile-service start:dev & yarn workspace post-service start:dev",
    "migration:run": "yarn workspace auth-service workspace:migration:run & yarn workspace profile-service workspace:migration:run & yarn workspace post-service workspace:migration:run"
  }
}
